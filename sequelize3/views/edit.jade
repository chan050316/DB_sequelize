body 
  h1 목록 추가하기
  hr

  form(method="post" action="/board/#{post.id}?_method=PUT")
    table
      tr
        td
          input(type="text" name="editTitle" value="#{post.title}")
      tr
        td
          input(type="text" name="editWriter" value="#{post.writer}")
    input(type="submit" value="수정하기")